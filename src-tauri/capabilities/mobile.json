{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Mobile companion app (mobile.html) — notifications only",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}

// window event names shared between the voice (STT) and speech (TTS) hooks —
// keep the string values identical, dispatchers/listeners in other files use
// the literals directly
export const TTS_STOP = "oc:tts-stop";
export const TTS_LIVE = "oc:tts-live";
